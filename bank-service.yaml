apiVersion: v1
kind: Service
metadata:
  name: bank-service
spec:
  type: NodePort
  selector:
    app: bank-app
  ports:
    - port: 8080        # Service port
      targetPort: 8080  # Container port
      nodePort: 30080   # External port
